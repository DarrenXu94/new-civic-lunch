---
import RatingImage from "./RatingImage.astro";

const { rating } = Astro.props;
---

<p>
  <RatingImage rating={rating} />
</p>

<a href={`/rating/${rating.trim().charAt(0)}/`}>
  Discover reviews from other diners with similar ratings!
</a>
